const Sidebar = () => {
  return (
    <div className="sb-inner">
      <div className="sb-head">
        <div className="sb-title">Wheno</div>
        <div className="sb-sub">Pick the right time for better outcomes.</div>
      </div>
      {/* We will build the rest of the sidebar later */}
    </div>
  );
};

export default Sidebar;
